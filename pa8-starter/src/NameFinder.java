import java.io.FileWriter;
import java.io.IOException;

public class NameFinder {
   public static void main(String[] args) throws IOException {
      /* TODO: initialize class variables
       * ---------------------
       * String csv_file: filename of csv data file
       * boolean write_to_file: determines if writing to file
       * String gender: determines if looking at male or female names
       * boolean max_heap: determines max or min heap
      */
      
      // TODO: initialize the buffer with the contents of 'csv file'

      // TODO: initialize the heap with the min or max heap comparator according to 'maxHeap'

      // TODO: add each data point to the heap using the buffer if matching desired 'gender'
      
      // TODO: write the heap to the file or print contents of heap to the console depending on 'write_to_file'
   }
}